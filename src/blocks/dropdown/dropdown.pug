mixin dropdown(textToggle, textTitle, classModifier)
    .dropdown(class!=classModifier)
        .dropdownTitle
            include ../title/title
            include __list/dropdown__list
            include __toggle/dropdown__toggle

            +title(textTitle, "")
        .dropdown__toggle
            +dropdown__toggle(textToggle)
        +dropdown__list(classModifier)
            