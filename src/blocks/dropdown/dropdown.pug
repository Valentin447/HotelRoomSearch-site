mixin dropdown(textFirstElement, textTitle)
    .dropdownTitle
        include ../title/title
        include ../arrow/arrow
        +title(textTitle, "")
        ul.dropdown(class!=attributes.class)
            li= textFirstElement
            +arrow('expand_more')(class= "arrow_dark-shade-50")